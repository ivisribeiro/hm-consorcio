# ===========================================
# Configuração Local (desenvolvimento)
# ===========================================

# Banco de dados PostgreSQL
DATABASE_URL=postgresql://crm_user:password@localhost:5432/crm_db

# JWT - Segurança
SECRET_KEY=sua-chave-secreta-minimo-32-caracteres-aqui
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30
REFRESH_TOKEN_EXPIRE_DAYS=7

# App
DEBUG=true

# CORS (URLs separadas por vírgula)
CORS_ORIGINS=http://localhost:5173,http://localhost:3000

# Email (opcional)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=

# Redis (opcional - deixe vazio para desabilitar)
REDIS_URL=

# ===========================================
# Configuração Render (produção)
# ===========================================
# DATABASE_URL -> Configurado automaticamente pelo Render
# SECRET_KEY -> Gerado automaticamente pelo Render
# DEBUG=false
# CORS_ORIGINS=https://seu-app.onrender.com
